@{
  Layout = "_Layout";
}

@* @using ToDoList.Models;

<div class="container">
  <h2>Category List</h2>

  @if (@Model.Count == 0)
  {
    <h4>Category List is EMPTY!</h4>
  } 
  else
  {
  <h4>* Click on Category to See Details *</h4>

 
    @foreach(Category category in Model)
    {
      <li>@Html.ActionLink($"{category.Name}", "Details", new { id = category.CategoryId }, new { @class="linkWordColor" }) </li>
    }
  


  <h5 class="pageLink">@Html.ActionLink("Item List", "Index", "Items", "Home")</h5>
  }
  
  <h5 class="pageLink">@Html.ActionLink("Add Category", "Create")</h5>
  
  <h5 class="pageLink">@Html.ActionLink("Home", "Index", "Home")</h5>

</div> *@